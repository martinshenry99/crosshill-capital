import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Crosshill Capital | Institutional-Grade Crypto Investment",
  description:
    "Invest in professionally managed crypto portfolios. Crosshill Capital offers institutional-grade investment management for individual investors.",
  keywords: ["crypto investment", "bitcoin", "ethereum", "portfolio management", "crosshill"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
